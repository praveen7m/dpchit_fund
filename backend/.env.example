# Create a new MongoDB Atlas cluster and replace these values:
PORT=5000
MONGODB_URI=mongodb+srv://newuser:newpassword@newcluster.mongodb.net/dpchits?retryWrites=true&w=majority
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
NODE_ENV=development

# Steps to create new MongoDB Atlas:
# 1. Go to https://cloud.mongodb.com
# 2. Create new project: "DpChitFund"
# 3. Create new cluster (free tier)
# 4. Create database user with username/password
# 5. Add IP address 0.0.0.0/0 to Network Access
# 6. Get connection string and update MONGODB_URI above